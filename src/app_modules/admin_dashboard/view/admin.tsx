'use client'
import { Stack, Title } from "@mantine/core";

export default function ViewAdminDashboard({ userId, }: { userId: any }) {
    return <>
        <Stack p={"lg"} spacing={"lg"}>


        </Stack>
    </>
}