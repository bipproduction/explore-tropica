'use client';
import { createTheme } from '@mantine/core';

export const theme = createTheme({
    /* Put your mantine theme override here */
    fontFamily: 'Bebas Neue, sans-serif, Montserrat, sans-serif',
    headings: {
        fontFamily: 'Bebas Neue, sans-serif, Montserrat, sans-serif',
    }
});